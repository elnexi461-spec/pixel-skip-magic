import { createFileRoute, useNavigate } from '@tanstack/react-router'
import { useState } from 'react'
import { Eye, EyeOff, Leaf, ShieldCheck } from 'lucide-react'
import { supabase } from '@/integrations/supabase/client'
import { lovable } from '@/integrations/lovable'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import panorama from '@/assets/mifugo-estate-panorama.jpg'
export const Route = createFileRoute('/auth')({ head: () => ({ meta: [{ title: 'Sign in — Mifugo Farm' }, { name: 'description', content: 'Sign in or create your secure Mifugo Farm estate account.' }, { property: 'og:title', content: 'Sign in — Mifugo Farm' }, { property: 'og:description', content: 'Access your animal estate, wallet and income.' }, { property: 'og:type', content: 'website' }, { name: 'twitter:card', content: 'summary_large_image' }] }), component: AuthPage })
function AuthPage() {
  const [mode, setMode] = useState<'login'|'signup'|'forgot'>('login'); const [show, setShow] = useState(false); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(''); const navigate = useNavigate()
  async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setBusy(true); setMessage(''); const fd = new FormData(e.currentTarget); const email = String(fd.get('email')); const password = String(fd.get('password')); try { if (mode === 'forgot') { const { error } = await supabase.auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/reset-password` }); if (error) throw error; setMessage('Check your email for the secure reset link.') } else if (mode === 'signup') { const { error } = await supabase.auth.signUp({ email, password, options: { emailRedirectTo: window.location.origin } }); if (error) throw error; setMessage('Check your email to confirm your account, then sign in.') } else { const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) throw error; await navigate({ to: '/dashboard' }) } } catch (err) { setMessage(err instanceof Error ? err.message : 'Please try again.') } finally { setBusy(false) } }
  async function google() { setBusy(true); const result = await lovable.auth.signInWithOAuth('google', { redirect_uri: window.location.origin }); if (result.error) { setMessage(result.error.message); setBusy(false); return } if (!result.redirected) await navigate({ to: '/dashboard' }) }
  return <main className="grid min-h-screen lg:grid-cols-[1.08fr_.92fr]"><section className="relative hidden overflow-hidden lg:block"><img src={panorama} alt="Mifugo Farm estate in the Kenyan highlands" className="absolute inset-0 h-full w-full object-cover" width={1920} height={960}/><div className="absolute inset-0 bg-gradient-to-t from-primary via-primary/20 to-transparent"/><div className="absolute inset-x-0 bottom-0 p-12 text-primary-foreground"><div className="mb-5 inline-flex items-center gap-2 rounded-md bg-background/90 px-3 py-2 text-xs font-bold text-primary"><Leaf/> Built for steady growth</div><h1 className="max-w-xl font-display text-6xl leading-[.96]">Your farm.<br/>Your future.</h1><p className="mt-5 max-w-lg text-lg text-primary-foreground/85">A beautiful, clear view of every animal, earning cycle and M-Pesa transaction.</p></div></section><section className="flex items-center justify-center bg-background px-5 py-10 sm:px-10"><div className="w-full max-w-md"><div className="mb-9 flex items-center gap-3"><div className="grid size-12 place-items-center rounded-full bg-primary text-2xl">🐄</div><div><p className="font-display text-2xl">Mifugo</p><p className="text-xs font-bold uppercase text-primary">Farm estate</p></div></div><h2 className="text-4xl">{mode === 'login' ? 'Welcome home' : mode === 'signup' ? 'Start your estate' : 'Reset access'}</h2><p className="mt-2 text-sm text-muted-foreground">{mode === 'login' ? 'Sign in to see your live farm account.' : mode === 'signup' ? 'Create your secure account in a minute.' : 'We’ll send a secure reset link to your email.'}</p>{mode !== 'forgot' && <div className="mt-7 grid grid-cols-2 rounded-md bg-muted p-1"><Button type="button" variant={mode === 'login' ? 'default':'ghost'} onClick={() => { setMode('login'); setMessage('') }}>Sign in</Button><Button type="button" variant={mode === 'signup' ? 'default':'ghost'} onClick={() => { setMode('signup'); setMessage('') }}>Sign up</Button></div>}<form onSubmit={submit} className="mt-6 space-y-4"><div><Label htmlFor="email">Email address</Label><Input id="email" name="email" type="email" autoComplete="email" required className="mt-2" placeholder="you@example.com"/></div>{mode !== 'forgot' && <div><div className="flex items-center justify-between"><Label htmlFor="password">Password</Label>{mode === 'login' && <button type="button" onClick={() => setMode('forgot')} className="text-xs font-bold text-primary">Forgot password?</button>}</div><div className="relative mt-2"><Input id="password" name="password" type={show ? 'text':'password'} autoComplete={mode === 'signup' ? 'new-password':'current-password'} minLength={8} required className="pr-11"/><button type="button" aria-label={show ? 'Hide password':'Show password'} onClick={() => setShow(!show)} className="absolute right-3 top-3 text-muted-foreground">{show ? <EyeOff className="size-5"/>:<Eye className="size-5"/>}</button></div></div>}{message && <p className="rounded-md border border-border bg-muted p-3 text-sm">{message}</p>}<Button size="lg" className="w-full" disabled={busy}>{busy ? 'Please wait…' : mode === 'login' ? 'Enter my estate' : mode === 'signup' ? 'Create account' : 'Send reset link'}</Button></form>{mode !== 'forgot' && <><div className="my-6 flex items-center gap-3 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border"/>or<span className="h-px flex-1 bg-border"/></div><Button size="lg" variant="outline" className="w-full" onClick={google} disabled={busy}><span className="text-lg font-bold">G</span> Continue with Google</Button></>}{mode === 'forgot' && <Button variant="ghost" className="mt-4 w-full" onClick={() => setMode('login')}>Back to sign in</Button>}<p className="mt-7 flex items-center justify-center gap-2 text-xs text-muted-foreground"><ShieldCheck className="size-4 text-primary"/> Protected account and encrypted payments</p></div></section></main>
}
